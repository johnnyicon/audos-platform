# Audos Platform — Issue Backlog

Issues and questions to raise with the Audos platform team. Close items by moving them to **Resolved** with a date and outcome.

---

## Open

| # | Raised | Area | Issue |
|---|--------|------|-------|
| 41 | 2026-08-05 | app-build | **A build job's output is truncated mid-JSON in the jobs list**, not at a record boundary and with no marker — half a small diagnostic payload was simply severed at `{"name…`. The `get_hook_logs` fallback was unavailable because the job had cleaned up its own temporary hook as instructed. Same shape as `#4`. Untested workaround: have the job write results into a workspace table instead of the job log. **Not filed with Audos yet.** See `blog/bugs/0039`. |
| 40 | 2026-08-05 | identity | **The Otto reachable over the external onboarding API has no `manage_server_functions` tool**, contradicting `docs/platform/07`, which documents it as *the* way server functions are created (including "if you're building tools or automating"). Another instance of the tool-surface split in `docs/platform/29` — likely signed-in-session-only. The only external creation route found is staging a Cursor delegation job. Otto's own self-report, not independently verified. Doc 07 corrected in place. **Not filed with Audos yet.** See `blog/bugs/0038`. |
| 39 | 2026-07-23 | media-gen | **`list_voiceover_voices` returns `undefined` for the `ID` field on ~82 of ~85 listed voices** — real names/accents/demographics come back, but only the 3 hardcoded fallback voices (Sarah `EXAVITQu4vr4xnSDxMaL`, Rachel `21m00Tcm4TlvDq8ikWAM`, Josh `TxGEqnHWrfWFTfGW9XjX`) are actually selectable via the documented `voiceId` param on `generate_voiceover`. The rest are listed but functionally inert. **Not filed with Audos yet.** From DoKnow. See `docs/platform/capabilities/media/generate-voiceover.md`, `blog/experiments/0032`. |
| 38 | 2026-07-23 | analytics-api | **`query_data_source(sourceId=funnel-events)` returns a degenerate 1-row/all-blank result while `query_events` correctly reports real event data for the identical workspace and time window in the same conversation.** Confirmed live against the DoKnow workspace: `query_events` (30d, by_type) returned 187 real events; `query_data_source(funnel-events)` returned exactly one record with every field (id/type/space/visitor/data/date) empty. Not yet root-caused — could be a different backing view/adapter, a pagination default of 1, or a broken path specific to this `sourceId`. **Not filed with Audos yet.** From DoKnow. See `docs/platform/capabilities/analytics/query-data-source.md`, `blog/experiments/0030`. |
| 37 | 2026-04-20 | db-api | **A workspace's `DB_API_KEY` was found hardcoded in client-side source, fully exposed in the compiled browser bundle** for any Audos CDN-deployed app using direct DB-hook access from React. Pre-existing, not introduced by porting — flagged as a standing warning: don't assume secrets referenced from React component code stay server-side. **Not filed with Audos yet.** From the Throughline project, not DoKnow/field-notes. See `blog/bugs/0037`. |
| 36 | 2026-07-10 | app-build | **Audos support chat flattens structured technical replies (line breaks, code blocks) into a single unreadable block**, making evidence like DNS records or API traces hard to review. Directly motivated `blog/feature-requests/0017` (ticket attachments + structured API), which was filed. **Not filed with Audos yet.** From Throughline. See `blog/bugs/0036`. |
| 35 | 2026-04-16 | ai-api | **The AI hook's `Generate()` call type has a hard ~1000-token response cap that `Chat()` on the same underlying model doesn't** — confirmed directly in code, with a PDF-extraction path deliberately built against `Chat()` to route around it. Also confirmed: the proxy only accepts PDF (not DOCX) on the `file` content block. **Team's own workaround confirmed working; underlying platform cap not changed.** From Throughline. See `blog/bugs/0035`. |
| 34 | 2026-04-19 | db-api | **The workspace's own direct-Postgres dev role (`ws_dev_*`) is DML-only** — `CREATE`/`DROP`/`ALTER TABLE` all rejected with permission errors. Any schema change still has to go through Otto in chat, defeating the purpose of direct DB access for a team doing programmatic schema management. **Not filed with Audos yet.** From Throughline. See `blog/bugs/0034`. |
| 33 | 2026-04-16 | app-build | **GitHub App connection broke entirely** (no OAuth redirect on Install/Relink, just an "Authentication required" toast), and separately **Otto's "New Chat" doesn't start a clean session** — it loads old conversation history instead. Neither independently re-verified as fixed. **Not filed with Audos yet.** From Throughline. See `blog/bugs/0033`. |
| 32 | 2026-04-18 | app-build | **`Desktop.tsx`'s hash router matches the entire hash, not a prefix** — `#app/subpath` doesn't match registered app `app`, so no deep links, no back/forward, no bookmarkable URLs are possible for any third-party app. A one-line fix (`hash.split('/')[0]`) was supplied informally; adoption unconfirmed. **Not filed with Audos yet.** From Throughline. See `blog/bugs/0032`. |
| 31 | 2026-04-09 | app-build | **A deliberately customized `Desktop.tsx` got silently reverted by a platform-initiated `[audos-sync]` commit — at least the third time the same fix has been undone.** No audit trail on the commit (no session ID, no prompt pointer) made root-causing it impossible; even Otto's own best guess couldn't be confirmed. Directly motivated two feature requests (file preservation, audit trail on sync commits). **Not filed with Audos yet.** From Throughline. See `blog/bugs/0031`. |
| 30 | 2026-04-16 | app-build | **Adding a React-dependent CDN package Audos doesn't pre-configure silently loads a second React and crashes (error #31).** Audos only auto-pins `?deps=react@...` for its own pre-configured packages; anything added manually gets a bare esm.sh URL that resolves its own peer React independently. Worked around by dropping the dependency (a 30-line hand-rolled router) rather than fixing the root cause. **Not filed with Audos yet.** From Throughline. See `blog/bugs/0030`. |
| 29 | 2026-07-08 | identity | **Otto's onboarding API rejected `Authorization: Bearer` auth for weeks while the documented body-auth form worked** — filed as a Priority Bug, marked `Completed` by Audos, but the Help-panel status wasn't trusted on its own: independently re-verified live on 2026-07-09 and again 2026-07-10 before treating it as actually fixed. **Filed with Audos 2026-07-08, confirmed fixed 2026-07-10.** From Throughline. See `blog/bugs/0029`. |
| 28 | 2026-07-17 | app-build | **Right now, no live path leads a cold visitor to field-notes' real content.** Walked the full end-to-end path in a cold headless browser to check whether the "old" email-then-password workaround still worked, since the invite-link rewrite is confirmed broken (`#27`). It doesn't. Submitting any email at the Audos gate goes straight through with no OTP — but instead of reaching the site's own `site_access` password screen, a fresh visitor lands on a generic, unbranded AI-assistant shell with a single sidebar app, "Build Findings Log," which reads an unrelated, empty table and shows 0 items. A direct DB check confirmed the real content (`content_items`, 6 rows) is intact — this is not data loss, it's that the currently-published entry path doesn't route to the Findings feed, the password gate, or Field Scout at all. Root cause not yet established; may share a cause with `#27` (stale published snapshot) or be a separate wiring/routing regression. **Not filed with Audos yet.** See `blog/bugs/0028`. |
| 27 | 2026-07-17 | app-build | **A landing-page rewrite job reported "Complete" — the live site kept serving the old code.** Dispatched a job to rewrite the platform-owned `EmailGate.tsx`; it marked itself `Complete` with a specific, confident outcome (file changed, version marker preserved, smoke-test passed). Three independent live checks — cold URL load, a bogus-token load, and reading the actual served source directly — all showed the old code was still live, with zero trace of anything from the new version. Likely cause: a second job dispatched at an overlapping time against the same file, plus evidence the live site was re-served from an already-published snapshot rather than the new build. The companion database change (a separate job) landed and was independently confirmed correctly — this is specifically a file/publish-path problem, not a general "changes don't stick" issue. **Not filed with Audos yet.** See `blog/bugs/0027`. |
| 24 | 2026-07-17 | identity | **In the "unified space" model, Audos's own EmailGate unconditionally wraps any custom app-level auth — with undocumented CRM/ad-pixel side effects.** A workspace with its own fully custom sign-in gate (a `site_access` table, username/password) still shows Audos's own email-capture prompt to a cold visitor first — confirmed by having Otto read the actual served source. The signed-out view of *every* workspace is the platform's own `components/EmailGate.tsx`; nothing else mounts until it reports `step === 'complete'`, so a custom gate can only ever be the second gate. Submitting the email step **registers a CRM contact and fires Meta/Reddit ad "Lead" pixels** — it's the platform's own lead-capture funnel, not a neutral auth screen. No config flag or Developer-panel toggle exists to disable or reorder it; the only fix is rebuilding `EmailGate.tsx` itself. **Re-verified live 2026-07-23 — still unchanged, and OTP itself no longer appears to be enabled on this workspace either: a fresh, never-used email walked straight into the app with no verification step at all.** **Not filed with Audos yet.** See `blog/bugs/0024`, `experiments/0022`. |
| 25 | 2026-07-17 | app-build | **Audos Code's chat input can silently fail to transmit a long, multi-paragraph message.** A ~3,000-character message with six structured content records, formatted as prose with blank lines, never reached the model — the agent correctly reported it hadn't received the data rather than fabricating content, and asked for a resend. The same content resent as compact single-line JSON transmitted correctly. Root cause unconfirmed (client paste handling? formatting quirk?). **Not filed with Audos yet.** See `blog/bugs/0025`. |
| 26 | 2026-07-17 | app-build | **Audos Code (Beta 0.3.0) failed to load 4 times across ~40 minutes of normal use** — two explicit load errors (recovered on retry), two stuck "connecting" states (one never recovered within the session). Low severity given the beta label and full recoverability, but a real, current reliability data point. **Not filed with Audos yet.** See `blog/bugs/0026`. |
| 19 | 2026-07-16 | db-api | **Workspace Database Access "Generate Credentials" is one-shot with no view/rotate path in the UI.** Credentials were generated once earlier in the same workspace session and the connection string wasn't captured at that moment. Every later click of the same button returns `409 Conflict: "Credentials already exist. Use regenerate to rotate them."` — but a full scan of the Developer panel's interactive elements found only the one `Generate Credentials` button; no regenerate/rotate/view control exists anywhere in the UI. `GET` on the same endpoint returns `401`, so it's not a read-back path either. Net effect: the feature becomes permanently unusable through the UI once generated once without saving the string. The 409's own error text references a "regenerate" capability the frontend never exposes, suggesting the backend supports rotation but the UI wasn't wired to call it. **Filed with Audos 2026-07-16, via Otto chat inside the field-notes workspace, status: pending.** Filed only after Otto independently confirmed (from the platform's own side, not just our UI reproduction) that it has no tool to view or rotate these credentials and that no "view" path exists for anyone. Priority bug report submitted, engineering notified, $27 bounty attached if confirmed as a real fix. **Audos marked this "Completed" 2026-07-17, claiming published to production — independently re-tested live 2026-07-22: still `409`/`401`, still one button, no change at all. Claim is false.** See `blog/bugs/0023-db-credentials-generate-once-no-recovery.md`, `blog/0016`. |
| 15 | 2026-07-16 | db-api | **`workspace_db_create_table` leaves an orphaned, unregistered table on a failed foreign-key constraint — no cleanup tool exists.** Building `field-notes` (in its own new workspace, `1d30572d-2ced-4dd1-872f-3e67a74891dd`), a `site_sessions` table with `access_id: uuid` referencing `site_access.id` failed: `Failed to create table: foreign key constraint "fk_app_site_sessions_access_id" cannot be implemented` — because (see `#16`) the platform silently makes every table's `id` a `serial` integer, never the requested `uuid`, so a uuid FK column can never reference it. That failure alone is a reasonable, explainable rejection. The real bug: **the failed call was not rolled back.** It left a real, physical `app_site_sessions` relation in the database that the platform's own catalog doesn't know about — every subsequent attempt to create `site_sessions` (including a corrected version with an integer `access_id`) fails with `Failed to create table: relation "app_site_sessions" already exists", and there is no `drop_table`/cleanup tool available to remove it. The table is stuck: not creatable, not droppable, not usable. Ask: (1) roll back partial DDL on constraint failure, or (2) at minimum, expose a way to drop an orphaned table. **Resolution used, 2026-07-16 (later): never actually cleared.** A ~2-hour follow-up job spent significant time trying to clear the orphan (see `#17`) before giving up and working around it — creating the sessions table under a new name, `site_sessions_v2`, rather than the originally-specified `site_sessions`. The orphaned `app_site_sessions` relation is still there, permanently unusable, as far as we know. Confirmed via direct read-only `db_list_tables`: workspace now has `content_items`, `site_access`, `site_sessions_v2` — not `site_sessions`. **Filed with Audos 2026-07-16 via Priority Support, status: pending.** |
| 17 | 2026-07-16 | security | **Possible raw-SQL restriction bypass in `db.rawQuery` — validator appears to check only the leading keyword, not the full statement. Not confirmed to actually execute a blocked operation; report as a finding to investigate, not a confirmed exploit.** While trying to clear the orphaned table from `#15`, a build job observed that `db.rawQuery`'s restriction to `SELECT`/`WITH`/`EXPLAIN` seemed to validate only the first keyword of the query string, meaning a chained statement (e.g. `SELECT 1; DROP TABLE ...`) might slip a write or DDL statement past the check. The job spent roughly 20 minutes testing this from multiple angles (transaction-scoped read-only wrappers, `set_config('transaction_read_only', ...)`, a third `options` argument to `rawQuery`) trying to use it to actually execute a `DROP TABLE` against the orphan. **It's not clear the bypass actually worked** — the job ultimately abandoned this path and worked around the orphan by creating `site_sessions_v2` instead (see `#15`), which suggests the bypass attempt did not succeed, or succeeded partially without producing a usable result. Worth Audos re-verifying the `rawQuery` validator's actual logic directly rather than trusting this secondhand account — if the leading-keyword-only check is real, it's worth closing regardless of whether this specific attempt got a DROP through. **Filed with Audos 2026-07-16 via Priority Support, status: pending.** |
| 18 | 2026-07-16 | app-build | **CORRECTED, 2026-07-16 (same day): this was a false alarm — our own verification method, not a platform data-loss bug. Original text below, kept for the record, followed by the correction.** ~~File-level changes made during a Cursor-delegation job may not be durably persisted if the job errors before completion — even after being live-tested successfully within that same run.~~ *(original finding, now retracted — see correction)*<br><br>**What we actually got wrong:** after job #83055 errored without a completion report, we checked `GET /api/space/{id}/files` and got `{"files": []}`, and concluded the job's file writes had been silently discarded. **That endpoint only ever reflects the *published* bundle — it has no visibility into draft/unpublished work at all.** We proved this directly: comparing the workspace's own Preview panel side-by-side, "Live" mode shows the old, never-updated marketing page (matching what the API returned), while "Draft" mode shows the actual built app — a real, working password gate. We logged in ourselves with the test credentials and reached the real content feed; the "Ask Field Scout" popup behaved correctly (small popup, not a shell teardown). **The app was there the whole time, in draft form — we were checking the wrong thing.** A follow-up job (see `field-notes/ACTIVITY-LOG.md`) confirmed the original job's app file, config edits, and hooks were already present when it started, and made one small fix (a session-expiry mismatch) rather than rebuilding from scratch — meaning the original job likely never actually failed at the level we thought; it just crashed before it *itself* found the right draft-preview URL to confirm and report its own success, and we then compounded that by checking the same wrong (published-only) endpoint independently. **Genuine remaining finding, downgraded from "bug" to "gotcha":** there is no obvious, documented way to distinguish "nothing was built" from "something was built but only exists in an unpublished draft" without knowing to check the Preview panel's Draft toggle specifically — the file-tree API silently returns empty in both cases. That's worth Audos documenting, but it is not data loss. **We've followed up on the original Priority Support ticket (already escalated to a human engineer) to correct the record before their engineering time is spent chasing a bug that isn't real.** |
| 16 | 2026-07-16 | db-api | **`workspace_db_create_table` always generates a `serial` (auto-increment integer) primary key named `id` — an explicit `uuid` id is rejected outright, undocumented.** Requesting `id: uuid` as a column returns `column "id" specified more than once` — the platform reserves `id` for its own auto-generated serial integer and will not let a caller override its type. `uuid` remains usable as a *non-primary-key* column type. Also undocumented: every table gets two more platform-injected columns beyond what's requested — `session_id` (text) and `updated_at` (timestamp, default `NOW()`). None of this is in the current column-type reference (`docs/platform/06`), which lists `uuid` as simply one of the supported types with no caveat about primary keys. Practical implication: any schema design that assumes a uuid primary key (ours did, for `field-notes`) needs a foreign-key column typed as `integer`, not `uuid`, to reference another table's `id`. **Filed with Audos 2026-07-16 via Priority Support, status: pending.** |
| 14 | 2026-07-16 | ai-api | **Correction, same day: a native embedding path DOES exist, undocumented — the "no capability" finding above was a false negative.** Originally filed after a round-3 sub-job swept `platform.integrations.isAvailable()` with guessed provider names (`openai-embeddings`, `vector-search`, etc.) — all `false` — and concluded nothing existed. A second round-3 sub-job tried the bare provider name instead: `isAvailable('openai')` → **`true`**, and `platform.integrations.proxy('openai', '/v1/embeddings', {method:'POST', headers:{...}, body: JSON.stringify({model:'text-embedding-3-small', input:'...'})})` **succeeded and returned a real 1,536-float OpenAI embedding — no API key supplied by the calling hook.** `proxy` is a generic authenticated passthrough to a fixed allowlist (`openai, stripe, twilio, heygen`, read from its own error message); Audos holds and injects the credential. **Revised ask:** this isn't "please build embedding support" anymore — it's **"please document `platform.integrations` and `proxy()`"**, since it's real, working, and currently discoverable only by introspecting the runtime and guessing the right call shape (body must be a JSON string, not an object — passing an object silently returns the platform's HTML index page with a 200, a dangerous false-success failure mode). A dedicated `platform.generateEmbedding()` convenience wrapper would still be nicer than hand-rolling the `/v1/embeddings` call shape, but is now a polish ask, not a capability gap. Independent of `#13` — this is about *producing* an embedding, `#13` is about *storing/searching* one. Side finding from the same probes, filed for awareness: the docs' "Available Globals" list is missing `platform.integrations`, `platform.externalFetch`, `platform.getLatestSession`, `session`, `respondRaw`, `db.bulkInsert` — all live and callable; and the "NOT Available (Security)" section claims `eval` is unavailable, but live enumeration shows `eval`, `Function`, `Proxy`, `Reflect`, `WebAssembly`, `SharedArrayBuffer` all present on `globalThis` (enumerated only, not invoked) |
| 13 | 2026-07-14 | db-api | **No native vector/embedding column type, and no way to enable pgvector — search/storage side only, not generation (see `#14`).** `workspace_db_create_table` rejects `vector(N)` (enum-restricted to `text/integer/bigint/decimal/boolean/timestamp/date/json/uuid`); raw SQL is SELECT/WITH/EXPLAIN-only, so `CREATE EXTENSION vector` can't be run. **Correction 2026-07-16:** pgvector 0.8.1 is *available in the catalog*, not "installed" — `installed_version` is null and it's absent from `pg_extension` (only `plpgsql` is actually installed). Also found: `vectorscale` 0.9.0 (DiskANN ANN index) is in the same available-not-installed state. **Both benchmark gaps now closed (round 2 + round 3, same day):** JSON-array + brute-force cosine similarity benchmarked at realistic row counts (50/300/1000) AND realistic dimensionality (1,536 floats, real OpenAI embedding size) — still fast: ~0.003ms/row scan time at 300–1,000 rows, 1–3ms total scan even at 1,000 rows; wall-clock is dominated by DB fetch (~250–540ms), not the comparison math. **Full picture as of 2026-07-16: producing an embedding is solved (`#14` — via `platform.integrations.proxy('openai', '/v1/embeddings', ...)`, no user API key needed); storing/searching it works via JSON + brute force at DoKnow's scale; only a dedicated vector column/ANN index for headroom at much larger scale remains unbuilt.** Still worth requesting native `vector`/`vectorscale` support for that headroom, but nothing here blocks DoKnow shipping today |
| 12 | 2026-07-14 | app-build | **Scheduled hooks are unreliable — confirmed inconsistent, not consistently broken.** 2026-07-14: two recurring hourly schedules created successfully (`POST /schedules`, valid RRULE, `status: pending`) never executed, one 2h07m overdue, the other 20m overdue, both `runCount: 0, lastError: null`; underlying hook confirmed working via manual call. **2026-07-16 retest:** a daily schedule fired correctly, within ~9s of `nextRun`, hook executed, `runCount` advanced 0→1. Same mechanism, opposite result — asked Otto directly whether the earlier non-fire is a known issue; it has no documented knowledge either way. Root cause (cadence-specific? since-resolved? per-workspace?) still unknown — genuinely open, not closed by either test alone. **No longer a hard blocker as of 2026-07-16**: client-orchestrated chaining (the app itself calling a hook repeatedly, pacing the work) was tested as a substitute — 5/5 sequential calls succeeded, ~1–1.8s each, zero rate-limiting/auth/concurrency friction. Anything that can tolerate the client staying open has a proven reliable path independent of the scheduler; only true unattended/fire-and-forget background work still needs the scheduler fixed. Two draft follow-up tasks were filed in the workspace itself (a bug report + a docs fix) but not run |
| 11 | 2026-07-14 | storage-api | **`/api/upload/image` silently corrupts non-image payloads.** Uploading a base64-encoded PDF through the image-upload endpoint returns `HTTP 200 {"success":true}` with a `.png` URL, but the stored bytes don't match what was sent (md5 mismatch, 3,031 → 3,050 bytes of bit-shifted garbage, zero common prefix). A real `image/png` payload round-trips correctly; the multipart endpoint (`/api/upload/file`) round-trips correctly for any content type. **Danger: this fails silently** — the caller gets a success response and a plausible-looking URL for corrupted data. Practical rule: any non-image file (PDF, audio, video) must go through `/api/upload/file` (multipart), never `/api/upload/image`, regardless of what the base64 endpoint's name might suggest is fine |
| 10 | 2026-07-14 | app-build | No way to stop/cancel a running Cursor delegation ("jobs-board") task once dispatched — genuinely no diagnostic or abort tool exists (`get_hook_logs`/`get_audos_code_status` don't apply, harness `TaskStop`/`TaskList` can't see jobs-board tasks). **Correction, same day:** the specific job that prompted this (#82122) turned out NOT to be hung — it completed at 45m01s after doing 67+ real tool actions across a thorough 5-test capability probe; that's just how long genuinely extensive work takes, not a stall. The underlying finding still stands (no way to check progress or cancel *while* a job is running, so a truly hung job is indistinguishable from a slow-but-working one until it finishes or fails) — just don't assume every long-running job is stuck |
| 9 | 2026-07-14 | app-build | "Cards due for review" count differs between two apps reading the same shared `learner_progress`/`review_items` data (`doknow-home` showed 12 cards due across 3 courses; `doknow-app`, verified same day with identical completed-lesson state, showed "No cards due yet"). Not yet root-caused — flagged twice in passing (`docs/platform/22-eliminating-the-chat-shell-playbook.md`, `blog/0011-taking-the-red-pill.md`) but never tracked here. Worth investigating: possible per-app scoping bug in how review items get scheduled/queried, rather than a genuine data difference |
| 8 | 2026-07-12 | app-build | Deep-linking a specific app via `#app-id` hash opened it *inside the chat shell* as a right-docked side panel, not full-screen. **Fixed 2026-07-13** in two rounds: (a) `Desktop.tsx`'s deep-link effect (~line 572) edited so `matchingApp` renders full-screen instead of `openPanel(...)` — verified for `#course-builder`. **Inconsistent for `#doknow-mockup-test`** (never resolved; app deleted, bug filed: `docs/platform/bug-reports/2026-07-13-deep-link-fullscreen-inconsistency.md`). (b) A second, related bug found live: the small "Assistant" pill meant to keep chat reachable (per the original fix's own brief) was instead calling `returnToAgentView()` in `Desktop.tsx`, which tore down the full-screen shell and returned the user to the **complete old three-pane chat interface** — confirmed by direct observation, not assumption (see `docs/platform/21-cursor-backend-research.md` for the correction). **Fixed and independently verified live 2026-07-13**: job #81716 (Cursor, model pinned to `fable-5`) changed the Assistant pill to open a small bottom-right popup instead of the full chat teleport; clicked it myself post-publish and confirmed the full-screen app stays untouched behind a compact chat drawer. (c) A third layer found the same day, this time by the user catching something our own "verify live" checks missed: the full-screen fix still **flashed the old shell on initial paint** — real, reproducible with a zero-delay screenshot, invisible to any check that waits a few seconds first. Root cause (Otto + job #81744, cited): `Desktop.tsx`'s `useState` defaults (`isSidebarOpen=true`, `activePanelId=null`, `mobileView='chat'`) render shell-mode first; a mount `useEffect` only corrects to full-screen *after* React's first paint — classic effect-driven-correction FOUC. **Fixed 2026-07-13, job #81755**: moved resolution into lazy `useState` initializers reading `window.location.hash`/config synchronously, no effect-driven correction. **Verified with the corrected method** (zero-delay screenshot, not the delayed check that missed it originally) across two apps, twice each — clean from first frame both times. Also: after any Desktop.tsx publish, a stale cached bundle can mask a fix for a while — a `?_cb=<value>` cache-bust reliably forces the fresh one, which explains the platform's own `_cb=`/`cdn=fallback` query params seen on this workspace. See `docs/platform/19-capabilities-and-limitations.md` |
| 7 | 2026-07-12 | app-build | `list_apps`/config can show an app as registered while the **served/compiled bundle is stale** (app missing from dock, route 404s) — a job reporting `published: yes` doesn't guarantee the live bundle reflects it, especially if a second job runs against the same app scope concurrently. See `docs/platform/19-capabilities-and-limitations.md` |
| 6 | 2026-07-12 | app-build | Cursor Background Agent jobs can instant-fail with `usage_limit_exceeded` (Audos's own shared Cursor account, not the workspace owner's) — blocks all builds until Audos's account headroom recovers. No user-facing status/ETA. See `docs/platform/19-capabilities-and-limitations.md` |
| 5 | 2026-07-12 | app-build | Job self-reports are unreliable — jobs have reported "complete," "published," and even explicit "verified" success language that did not match the live app when independently re-tested in-browser. No regression observed, but the reported verification doesn't mean what it says. See `docs/platform/19-capabilities-and-limitations.md` |
| 4 | 2026-07-12 | app-build | No tool/API to retrieve a build job's full completion log — long reports truncate mid-sentence with no way to expand, which repeatedly hid the actual root-cause detail. See `docs/platform/19-capabilities-and-limitations.md` |
| 3 | 2026-03-31 | web-api | `analyze` action: `research` object not returned — response shape appears incomplete or action not fully deployed |
| 2 | 2026-03-31 | web-api | `isJsRendered` boolean field missing from `fetch` response — warning text fires correctly but the flag itself is undefined |
| 1 | 2026-03-31 | web-api | `contentLength` from `fetch` on JS-rendered SPAs returns just the title length — need metadata/analyze as workaround *(partially resolved by changelog 20260331-1119, but isJsRendered flag still broken)* |

---

## Resolved

| # | Raised | Resolved | Area | Issue | Outcome |
|---|--------|----------|------|-------|---------|
| R3 | 2026-03-31 | 2026-03-31 | analytics-api | `URLSearchParams is not defined` crash | Fixed — rewrote with manual `buildQuery()` helper |
| R2 | 2026-03-31 | 2026-03-31 | web-api | `response.headers.get is not a function` | Fixed — reads full response as text now |
| R1 | 2026-03-31 | 2026-03-31 | db-api | Insert into `dashboard_activity` failed — missing required `title` field | Documented schema, `title` is required |
