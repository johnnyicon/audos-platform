# Other media tools — schema confirmed, not yet run

**Status: 📄 documented, unverified.** Otto-chat-triggered tools (see
`../../29-otto-tool-surface-vs-app-callable-hooks.md`).

| Tool | Notes |
|---|---|
| `redesign_logo` | Params: `feedback`, `style` (flat/outlined/emoji/material), `conversationContext`. Runs as a subagent with a 3-iteration critique loop, per schema. Paid gen. Not run — deprioritized behind video/voiceover/image. |
| `list_carousel_posts`, `get_carousel_post(postId)`, `get_carousel_templates`, `export_carousel(postId)` | Instagram carousel generation/export. Export renders only, no publish. Not run. |

Source: Otto chat, 2026-07-23.
